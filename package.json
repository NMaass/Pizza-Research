{
  "name": "pizza-research",
  "private": true,
  "workspaces": ["client", "shared", "worker"],
  "scripts": {
    "dev:worker": "cd worker && bunx wrangler dev",
    "dev:client": "cd client && bunx vite",
    "deploy:worker": "cd worker && bunx wrangler deploy",
    "deploy:client": "cd client && bun run build && bunx wrangler pages deploy dist --project-name=pizza-research",
    "build:client": "cd client && bunx vite build",
    "typecheck": "cd worker && bunx tsc --noEmit && cd ../client && bunx tsc --noEmit"
  }
}
